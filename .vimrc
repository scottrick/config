set nocompatible

syntax enable
filetype plugin on

" Search subfolders, provides tab completion for file tasks
set path+=**

" display all matching files when searching
set wildmenu

set incsearch
set hlsearch

